from util import helpers

def test_flatten():
    list = [[1,2,3],4,5]
    flattened_list = helpers.flatten(list)
    assert flattened_list == [1,2,3,4,5]

def test_flatten_default():
    list = [1,2,3,4,5]
    flattened_list = helpers.flatten(list)
    assert flattened_list == [1,2,3,4,5]
